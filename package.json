{
  "name": "json-server",
  "version": "0.0.1",
  "description": "Armaremos un pequeño servidor con informacion de cursos",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json"
  },
  "author": "Matias Monteros",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
